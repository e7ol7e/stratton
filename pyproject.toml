[project]
name = "vocabulary-bot"
version = "0.1.0"
description = "Telegram vocabulary trainer built with aiogram 3"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "aiogram>=3.28,<4",
    "aiosqlite>=0.21,<1",
    "python-dotenv>=1.1,<2",
]

[tool.uv]
package = false
